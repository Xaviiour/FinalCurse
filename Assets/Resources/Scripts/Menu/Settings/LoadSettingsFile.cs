using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class LoadSettingsFile
{
    public static SO_Settings settings;
    public static SO_Settings defaultSet;

    public static void InitiateSettings()
    {
        //SettingsFile data = SnLSettings.LoadSettings();

        /*if (data != null)
        {
            settings.brightValue = data.brightSet;
        }
        else
        {
            settings.brightValue = defaultSet.brightValue;
        }*/
    }
}
